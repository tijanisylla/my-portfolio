// Interface for data
export interface myData {
  id: number;
  title: string;
  url: string;
  description: string;
  img_url: string;
}

export interface MyArray {
  name: string;
  icon: string;
}
